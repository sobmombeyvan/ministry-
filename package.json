{
  "private": true,
  "scripts": {
    "dev": "powershell -ExecutionPolicy Bypass -File ./run.ps1",
    "dev:direct": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix frontend"
  }
}
